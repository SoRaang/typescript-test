export type User = {
    id: string,
    name: string,
    address: string,
    email: string,
    description?: string
}

export type Product = {
    id: number,
    name: string,
    price: number,
    description?: string
}